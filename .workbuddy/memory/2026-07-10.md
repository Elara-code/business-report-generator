# 2026-07-10

## 商业分析报告生成器（WorkBuddy AI · Agent 应用）

**项目位置**: `/Users/liuyawei/WorkBuddy AI/2026-07-10-01-47-41/`

**用户需求**: 输入领域/行业 → 生成图文并茂的商业模式分析报告；输入产品 → 生成产品拆解/竞品对比报告。

**已交付**:
- `report-engine/` 命令行生成器：Python 3.13 venv，含 3 个 prompt 模板、7 个 SVG 模板（条形/折线/雷达/漏斗/价值链/2x2象限/商业模式画布）、3 种格式渲染器（HTML/MD/PDF）
- `web/` 单页应用：左侧表单 + 右侧实时预览 + 历史报告抽屉
- `reports/` 已生成 3 份示范报告：咖啡行业（8 章 6 图）/ Notion 产品（8 章 7 图）/ Notion vs Obsidian 竞品（9 章 9 图）
- 预置 mock 模式，无需 API Key 即可看效果；`--ai openai` 支持 OpenAI 兼容协议（DeepSeek/通义千问等）
- 本地服务已启动在 http://127.0.0.1:8765（task_id: 5Sf1ui）

**技术栈**:
- Python 3.13 (managed venv)
- 纯 SVG 矢量图（零图表库依赖）
- Vanilla JS 网页（无构建）
- weasyprint PDF（macOS 需 `brew install pango libffi`）

**踩坑**:
- `str.format` 与 prompt 模板里的花括号冲突，改用 `.replace()` 解决
- mock 模式应跳过 prompt 加载直接读 `examples/*.json`
- weasyprint 缺系统库时只在 PDF 路径报错，HTML/MD 不受影响（已做容错）

**用户偏好**:
- 中文环境
- 偏好 light theme UI（主色 #2563eb，accent #f97316）
- 喜欢双形态交付（CLI + Web）
- 要"图文并茂"（强调 SVG 矢量图，不接受纯文本）

## GitHub 推送（17:55）

- 仓库：`https://github.com/Elara-code/business-report-generator`（public）
- 提交：`cb3bcda` 31 个文件（不含 venv / reports / 内存目录外的内容）
- `.gitignore` 排除：`.venv/`、`__pycache__/`、`reports/`、`*.log`、`*.tmp`、`.DS_Store`
- git 身份：`Elara-code` / `elara-code@users.noreply.github.com`（noreply 邮箱，不暴露真邮箱）
- 推送后从 remote URL 清除了 token 敏感信息

## 第一阶段稳定性修复（18:18，基于代码审阅）

**审阅来源**：用户给了一份非常详细的代码审阅，指出 P0/P1 问题 + 产品/工程建议。

**已修复（commit `ddac96c`）**：

P0：
- 根路由 `/` → `web/index.html`
- 漏斗图 `f'{esc(v):,}'` bug
- Mock preset 错配 → `coerce_report()` 强制覆盖 `meta.type` / `meta.subject`
- `--from-json` 流程补全（之前 `report = extract_json(raw)` 漏写）
- Pydantic 运行时校验（`report_model.py`），LLM 缺字段自动兜底

P1：
- `/api/history` 只返回真实存在的文件（用 `os.path.exists` 过滤）
- LLM 调用支持 timeout / retry
- 报告目录加微秒 + 6 字符 UUID 后缀（`make_out_dir`）
- HTML 渲染器用 bleach 清洗 LLM 输出（剥 `<script>` / `javascript:`）
- `WorkBuddyProvider` 明确为 `MockProvider` 别名

**测试**：`report-engine/tests/test_fixes.py` 20 个用例全过

**下一步**（用户路线图）：P2 可信度（URL / 置信度 / 事实 vs 推断标签），P3 输入丰富度，P4 报告后处理，P5 工程化（CI / Docker / LICENSE）。

## 服务重启（18:32）

用户报告 `127.0.0.1:8765` 无法访问。原因：之前 `&` 启动的后台进程在 Bash 工具的 sandbox 隔离下被清理掉了。
解决：改用 `nohup ... & disown` 启动，nohup 进程脱离 shell session，验证 3 个端点（`/`、`/web/index.html`、`/api/history`）全部 200。

